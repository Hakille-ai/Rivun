# Driver Manifests

The multi-node example writes signed driver manifests here:

- `echo.manifest.toml`;
- `thermostat.manifest.toml`.

They are generated from the commands in the parent README and should not be
copied between real environments without verifying the author key.
