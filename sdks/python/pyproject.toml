[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "rivun-sdk"
version = "0.1.0"
description = "Lightweight Python SDK for Rivun protocol control envelopes and RivunStore payloads"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
authors = [{ name = "Rivun contributors" }]

[project.optional-dependencies]
crypto = ["blake3>=1.0.0", "PyNaCl>=1.5.0"]

[tool.setuptools.packages.find]
where = ["src"]
